MAIL_PORT = 587
MAIL_USE_SSL = False
MAIL_USE_TLS = True
UPLOAD_FOLDER = './local/data/uploads'
STATIC_FOLDER = './static'

# Local development settings
DEBUG = True
TESTING = False

# Local database (SQLite for development)
SQLALCHEMY_DATABASE_URI = 'sqlite:///local_database.db'

# Local Celery settings (Redis for local development)
CELERY_BROKER_URL = 'redis://localhost:6379/0'
CELERY_RESULT_BACKEND = 'redis://localhost:6379/0'

# Local mail settings (you can use a service like Mailtrap for testing)
MAIL_SERVER = 'smtp.gmail.com'
MAIL_USERNAME = 'chrisericksonlolol@gmail.com'
MAIL_PASSWORD = 'hnangblfpeyylmxt '
MAIL_DEFAULT_SENDER = 'chrisericksonlolol@gmail.com'

# Local secrets (change these for production)
SECRET_KEY = 'dev-secret-key-change-in-production'
URL_SAFETIMEDSERIALIZER = 'dev-serializer-key-change-in-production'
EMAIL_CONFIRMATION_SALT = 'dev-email-salt-change-in-production'
RESET_PASSWORD_SALT = 'dev-reset-salt-change-in-production'

# Local Azure settings (you can disable these for local testing)
AZURE_CONNECTION_STRING = 'your-azure-connection-string'
LOG_CONTAINER_NAME = 'your-log-container'
EXPORT_CONTAINER_NAME = 'your-export-container' 